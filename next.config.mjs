/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com',"s3.ap-southeast-1.amazonaws.com","feelings-next.netlify.app"], // Replace 'example.com' with your domain
      },
};

export default nextConfig;
